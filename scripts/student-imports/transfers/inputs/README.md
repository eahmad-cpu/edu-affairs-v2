# Student transfer input

Create `student-transfer.xlsx` with a worksheet named `students` and exactly these headers:

```text
studentId,nationalId,targetSchoolId,targetAcademicYearId,targetClassId,transferReason
```

Use the inspect script before any preview or apply operation.
